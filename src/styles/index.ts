import { createStitches } from "@stitches/react";

export const {
  config,
  styled,
  globalCss,
  keyframes,
  theme,
  css,
  createTheme,
  getCssText,
} = createStitches({
  theme: {
    colors: {
      rocketseat: "#8257e6",
    },
  },
});
